{
  "name": "moderation-ledger",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "tsx src/migrate.ts",
    "test": "node --test --import tsx test/*.test.ts",
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/server.js",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "fastify": "5.6.0"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "tsx": "4.20.5",
    "@types/node": "22.18.1"
  }
}
