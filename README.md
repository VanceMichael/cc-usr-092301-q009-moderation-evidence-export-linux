# 内容复核证据封存导出

这是未成年人内容处置复核账本的后端基线，使用 Node.js、TypeScript、Fastify 与 SQLite，已有健康接口、迁移入口和自动化测试。

## 本地验证

执行测试：

```bash
npm test
```

执行编译或构建检查：

```bash
npm run build
```

所有验证均在单个 Linux 应用环境中完成，不需要浏览器或独立运行的数据库、缓存与消息队列。
